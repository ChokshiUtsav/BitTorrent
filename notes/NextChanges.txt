Next Changes :

1) Add last_request_time in tracker structure.
2) last_seen in peer must be updated and if it goes beyond keep-alive time (2 minutes), send keep-alive message
3) define message structure
   struct message
   		length dd ?
   		type   db ?
   		payload dd ?
4) define piece structure
	struct piece
		sha1 rb 20
		length dd ?
		filename dd ?
5) define torrent_thread structure
	struct torrent_thread
		thread_id dd ?
		thread_type dd ?

6) update torrent to include number of threads managed by it and array of threads
	threads_cnt dd ?
	threads     dd ?